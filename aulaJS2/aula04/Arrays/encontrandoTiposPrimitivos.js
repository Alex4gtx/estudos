// encontrando elementos (primitivos)
const numeros = [1,2,3,1,4];

numeros.indexOf(2); //mostra o indice dos elementos
numeros.lastIndexOf(1); //procura a ultima ocorrencia do elemento e retorna seu indice
numeros.includes(2); // retorna verdadeiro se o elemento estiver na lista